version: 2
sources:
  - name: stripe
    database: raw
    tables:
      - name: payments
        columns:
          - name: id
            tests:
              - unique
              - not_null
      